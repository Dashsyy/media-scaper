# TODO
- Add analyze SSE stream to avoid polling.
- Persist analyze/download jobs in PostgreSQL.
- Add history cleanup actions (clear, export).
- Add optional cookies upload for restricted Facebook content.
